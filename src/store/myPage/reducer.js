// NOTE: I decided to write this case in user reducer since I need the state of user reducer as a initial state

// import { STORY_POST_SUCCESS } from "../user/actions";

// const initialState = {};

// export default (state = initialState, action) => {
//   switch (action.type) {
//     case STORY_POST_SUCCESS:
//       return {...state , ...action.payload}

//     default:
//       return state;
//   }
// };
